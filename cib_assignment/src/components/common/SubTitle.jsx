import React, { Component } from 'react';
import './SubTitle.css';

const SubTitle = (props) => {
    return ( 
        <h4 className='subTit'>{props.SubTitle}</h4>
     );
}
 
export default SubTitle
;